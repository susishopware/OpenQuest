{
  "name": "@openquest/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build-data": "tsx scripts/build-data.ts",
    "start": "tsx --env-file-if-exists=../../.env src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@openquest/adapter-de-muenster": "workspace:*",
    "@openquest/adapter-de-nrw": "workspace:*",
    "@typesafe-ai/sdk": "^0.6.0",
    "maplibre-gl": "^5.6.0"
  },
  "devDependencies": {
    "undici": "^7.30.0",
    "vitest": "^3.2.0"
  }
}
