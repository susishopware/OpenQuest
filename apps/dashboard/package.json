{
  "name": "@openquest/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx --env-file-if-exists=../../.env src/server.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@openquest/tree-search": "workspace:*",
    "maplibre-gl": "^5.6.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
